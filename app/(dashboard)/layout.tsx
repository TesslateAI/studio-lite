// This file is the mandatory file-system layout for the (dashboard) route group.
// It should be simple and not export anything other than the default function.
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}